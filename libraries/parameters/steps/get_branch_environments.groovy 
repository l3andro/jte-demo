package environments

class BranchEnvironments implements Serializable {
    static Map get(Map branchEnvironments, String branch) {
        
        def environments = branchEnvironments.get(branch, branchEnvironments.get('desenvolvimento'))

        //environments.each {
       //     key, value -> def env.key = value
       // }

        //return branchEnvironments.get(branch, branchEnvironments.get('desenvolvimento'))
    }
}