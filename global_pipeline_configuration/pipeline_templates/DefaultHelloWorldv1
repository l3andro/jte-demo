//library 'libTest@' + pipelineConfig.keywords.library_version

//build(pipelineConfig.keywords.build_tool)

def function="build"
def library = jte.libraries."libraries"
def stepFile = library."$libStep"
stepFile."$function" ()